export interface User {
  email?: string | null,
  phone?: string | null,
  role?: string | null,
  firstName?: string | null,
  secondName?: string | null,
  image?: string | null,
  error?: string | null,
}
